# This file sample workflow file
#workflow
name: hello
on: workflow_dispatch

# job should we inside the workflows
jobs:
  say-hello:
    runs-on: ubuntu-latest
# step should inside the jobs and it could we multiple 
    steps:
      - name: say hello to everyone
        run: echo "hello dosto"
        
